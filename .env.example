# Copy to .env and fill in. .env is git-ignored — never commit it.
# Only non-secret config goes here. The Canvas token is stored separately in the
# OS keychain via `canvas-copilot login`.

# Your institution's Canvas API base URL, including /api/v1
# e.g. https://canvas.cmu.edu/api/v1
CANVAS_BASE_URL=
